import React from "react";
import Poll from "./Poll";

const PreviousPolls = (props) => {
    return (
        <div>
            Previous Polls
            <Poll />
        </div>
    );
}

export default PreviousPolls;